# 🚀 GraphQL + Express + Sequelize Todo API

This project demonstrates how to build a **GraphQL API** on top of a SQL database using **Express.js** and **Sequelize ORM**.  
It exposes CRUD operations (`create`, `read`, `update`, `delete`) for a simple `Todo` model.

---


Please refer to examples for checking out the syntactic sugaaaaa
